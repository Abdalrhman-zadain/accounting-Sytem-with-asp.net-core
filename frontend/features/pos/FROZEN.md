# Restaurant POS — maintenance mode

This folder owns **restaurant POS** only (`/pos/*`).

## Rules

- Bugfixes only unless the user explicitly requests a restaurant POS change.
- Do **not** add market/retail POS features here.
- Do **not** import from `frontend/features/pos-market/`.

## Market POS

All new market/retail POS work belongs in `frontend/features/pos-market/`.

See `docs/pos-market/README.md`.
